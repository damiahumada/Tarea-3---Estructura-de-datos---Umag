#ifndef FILE_READER_H
#define FILE_READER_H
#include <stdlib.h> 
/*
 * Esta funcion es la encargada de leer la secuencia de adn.
 * Lee la secuencia S de un archivo y la devuelve como un string (char*).
 */
char* leer_secuencia(const char* nombre_archivo);

#endif